import { install, Options } from './store/Store'
import Database from './database/Database'
import Model from './model/Model'

export { install, Options, Database, Model }

export default {
  install,
  Database,
  Model
}
